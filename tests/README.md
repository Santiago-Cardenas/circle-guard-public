# Tests del Taller 2

```
tests/
├── e2e/       # REST Assured E2E sobre el stack desplegado en K8s (M5c)
└── perf/      # Locust scenarios (M5d)
```

Las pruebas unitarias e integración viven dentro de cada microservicio en
`services/<svc>/src/test/...` (M5a y M5b).
